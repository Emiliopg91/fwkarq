[package]
name = "rust_framework"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "^0"
filetime = "^0"
reqwest = { version = "^0", features = ["blocking"] }
serde = "^1"
serde_derive = "^1"
serde_json = "^1"
serde_yaml = "^0"
thiserror = "^2"
toml = "^1"

[dev-dependencies]
uuid = { version = "1", features = ["v4"] }

[lib]
doctest = false
